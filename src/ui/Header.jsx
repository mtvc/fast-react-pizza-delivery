import { Link } from "react-router-dom";

function Header() {
  return (
    <header>
      <Link to="/">Fast React Pizza Delivery</Link>

      <p>Pizza's</p>
    </header>
  );
}

export default Header;
